#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int display( ){
printf ( "\nFools too use C!" ) ;
main( ) ;
}


int main(int argc, char *argv[]) {
//printf ( "\nOnly stupids use C?" ) ;
//display( ) ;

//int a=4;
//printf("%d %d %d",a,++a,a++);
//beacuse when we give the argument in printf statement the compiler reads right to left 
	return 0;
}
